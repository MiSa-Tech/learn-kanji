spring:
  application:
    name: learn-kanji-test
logging:
  level:
    org.springframework.data.neo4j.cypher.unrecognized: OFF
    org.springframework.security: DEBUG

security:
  jwt:
    secret-key: test-secret-key
    expiration-time: 3600000